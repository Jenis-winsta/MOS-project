import 'package:flutter/material.dart';

class LogoutPage extends StatelessWidget {
  // const LogoutPage({super.key});
  static const String id = "logout-page";

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
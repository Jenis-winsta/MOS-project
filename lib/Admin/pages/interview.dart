import 'package:flutter/material.dart';

class InterviewPage_a extends StatelessWidget {
  //const DashboardPage({super.key});


  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child:Text("Interview page"),
        ),
    );
  }
}
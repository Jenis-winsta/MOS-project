import 'package:flutter/material.dart';

class ReportsAnalysisPage_a extends StatelessWidget {
  //const DashboardPage({super.key});


  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child:Text("REports and analysis page"),
        ),
    );
  }
}